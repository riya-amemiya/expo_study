import styled from "styled-components/native";
import { rem } from "@/utils/responsive";

export const Title = styled.Text`
  font-size: ${rem(2)}px;
  font-weight: bold;
  margin-bottom: ${rem(3.125)}px;
  text-align: center;
  color: #333;
`;
