import styled from "styled-components/native";

import { rem } from "@/utils/responsive";

export const Container = styled.View`
  flex: 1;
  padding: ${rem(1.875)}px;
  background-color: #f8f8f8;
  padding-top: ${rem(5)}px;
`;
